{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { IFS= read -r file_path; if [ -n \"$file_path\" ] && echo \"$file_path\" | grep -qE '\\.(c|cpp|cc|cxx|h|hpp|hxx)$'; then clang-format -i -- \"$file_path\"; fi; }"
          }
        ]
      }
    ]
  }
}
